import { MarkSelectDirective } from './mark-select.directive';

describe('MarkSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkSelectDirective();
    expect(directive).toBeTruthy();
  });
});
